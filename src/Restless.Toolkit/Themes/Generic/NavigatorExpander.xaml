<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:keys="clr-namespace:Restless.Toolkit.Resource"
    xmlns:con="clr-namespace:Restless.Toolkit.Controls">

    <Style x:Key="ExpanderToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="Transparent">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ExpanderPathStyle" TargetType="Path">
        <Setter Property="Fill" Value="DarkSlateGray"/>
        <Setter Property="Stretch" Value="Uniform"/>
        <Setter Property="Height" Value="15"/>
        <Setter Property="Width" Value="15"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Geometry x:Key="ChevronDownGeometryKey">M7.41,8.58L12,13.17L16.59,8.58L18,10L12,16L6,10L7.41,8.58Z</Geometry>
    <Geometry x:Key="ChevronRightGeometryKey">M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z</Geometry>

    <Path x:Key="DefaultIcon" x:Shared="false"  Style="{StaticResource ExpanderPathStyle}" Data="{StaticResource ChevronRightGeometryKey}"/>
    <Path x:Key="ExpandedIcon" x:Shared="false"  Style="{StaticResource ExpanderPathStyle}" Data="{StaticResource ChevronDownGeometryKey}"/>

    <ControlTemplate x:Key="NavigatorExpanderTemplate" TargetType="con:NavigatorExpander">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <ToggleButton
                x:Name="ControlButton"
                Style="{StaticResource ExpanderToggleButtonStyle}"
                Margin="{TemplateBinding ButtonMargin}"
                Content="{TemplateBinding ActiveIcon}"
                Cursor="{TemplateBinding ButtonCursor}"
                Foreground="{TemplateBinding Foreground}"
                VerticalAlignment="Center"
                IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>

            <ContentPresenter
                Grid.Column="1"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="Center"
                Content="{TemplateBinding Header}"/>

            <ContentPresenter
                x:Name="ControlContent"
                Grid.Row="1"
                Grid.ColumnSpan="2"
                Margin="{TemplateBinding Padding}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                Focusable="False"
                Visibility="Collapsed"/>
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="IsExpanded" Value="True">
                <Setter TargetName="ControlContent" Property="Visibility" Value="Visible" />
            </Trigger>
        </ControlTemplate.Triggers>

    </ControlTemplate>

    <Style x:Key="{x:Static keys:ResourceKeys.DefaultNavigatorExpanderStyleKey}" TargetType="con:NavigatorExpander">
        <Setter Property="Template" Value="{StaticResource NavigatorExpanderTemplate}"/>
        <Setter Property="Icon" Value="{StaticResource DefaultIcon}"/>
        <Setter Property="ExpandedIcon" Value="{StaticResource ExpandedIcon}"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="ButtonCursor" Value="Hand"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="con:NavigatorExpander" BasedOn="{StaticResource {x:Static keys:ResourceKeys.DefaultNavigatorExpanderStyleKey}}"/>

</ResourceDictionary>